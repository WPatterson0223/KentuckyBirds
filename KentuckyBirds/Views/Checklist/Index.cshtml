@model IEnumerable<Checklist>

<h2>Birds</h2>

<div style="display: block">
    <a href="/Checklist/InsertBird/">Add a New Bird</a>
</div>

<div class="col-1 p-0 justify-content-end">
    <form asp.controller="Bird" asp.action="Index">
        <span class="btn.group">
            <input class="form control" type="text" name="userSearch" placeholder="Search">
            <input class="p-1 m1-2 btn btn-sm btn-info" type="submit" value="Search" />
        </span>

    </form>
</div>

<table class="table">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Latin Name</th>
        <th>Length</th>
        <th>Height</th>
        <th>Wingspan</th>
        <th>Comment</th>
    </tr>

    @foreach (var bird in @Model)
    {
        <tr>
            <td>@bird.ID</td>
            <td><a href=/Checklist/ViewBird/@bird.ID>@bird.Name</a></td>
            <td>@bird.LatinName</td>
            <td>@bird.Length</td>
            <td>@bird.Height</td>
            <td>@bird.Wingspan</td>
            <td>@bird.Comment</td>
        </tr>
    }

</table>

<div style="display: block">
    <a href="/Checklist/InsertBird/">Add a New Bird</a>
</div>

